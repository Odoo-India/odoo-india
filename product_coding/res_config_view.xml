<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <!-- Product Config Settings Form View -->
        <record id="view_product_configuration" model="ir.ui.view">
            <field name="name">product.settings</field>
            <field name="model">product.config.settings</field>
            <field name="arch" type="xml">
                <form string="Configure Products" class="oe_form_configuration" version="7.0">
                    <header>
                        <button string="Apply" type="object" name="execute" class="oe_highlight"/>
                        or
                        <button string="Cancel" type="object" name="cancel" class="oe_link"/>
                    </header>
                    <separator string="Coding Process"/>
                    <group>
                        <label for="id" string="Coding Settings"/>
                        <div>
                            <div>
                                <label for="default_coding_method"/>
                                <field name="default_coding_method" class="oe_inline"/>
                            </div>
                        </div>
                    </group>
                </form>
            </field>
        </record>

        <!-- Product Config Settings Action -->
        <record id="action_product_configuration" model="ir.actions.act_window">
            <field name="name">Configure Products</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">product.config.settings</field>
            <field name="view_mode">form</field>
            <field name="target">inline</field>
        </record>

        <!-- Product Config Settings Menu -->
        <menuitem id="menu_product_config" action="action_product_configuration" name="Products" parent="base.menu_config" sequence="1"/>

    </data>
</openerp>
